module Drill.D07Monad.SimpleIO where

-- | 標準入力から名前を読み込み、"Hello, <名前>!" と出力する IO アクション。
--
-- ヒント: getLine で入力を受け取り、putStrLn で出力する
--
-- 実行例:
--   > greetUser
--   （入力） Alice
--   Hello, Alice!
greetUser :: IO ()
greetUser = undefined

-- | 名前を受け取り挨拶文字列を作る純粋ヘルパー関数。
-- greetUser の内部で使うことを想定している。
--
-- 例:
--   makeGreeting "Alice"   == "Hello, Alice!"
--   makeGreeting "Haskell" == "Hello, Haskell!"
makeGreeting :: String -> String
makeGreeting = undefined
